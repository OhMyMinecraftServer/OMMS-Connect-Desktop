<Window
    x:Class="OMMS_Connect_Desktop.MessageBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OMMS_Connect_Desktop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar">
            <Image Source="assets/omms-icon.jpg"
                   HorizontalAlignment="Left" 
                   Width="20" Height="20" 
                   Margin="10,0"
            />
            <TextBlock x:Name="AppTitleTextBlock" Text=""
                       TextWrapping="NoWrap"
                       FontSize="16"
                       Style="{StaticResource CaptionTextBlockStyle}" 
                       VerticalAlignment="Center"
                       Margin="40,0,0,0" FontFamily="Consolas"/>
        </Grid>

        <TextBlock x:Name="TextBlock" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"></TextBlock>
    </Grid>
</Window>
